{
  "name": "tempmail",
  "version": "1.0.0",
  "description": "Disposable email service powered by Cloudflare Workers",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "pages:deploy": "wrangler pages deploy public/"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
